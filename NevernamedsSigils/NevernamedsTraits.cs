using DiskCardGame;
using System;
using System.Collections.Generic;
using System.Text;

namespace NevernamedsSigils
{
   public static class NevernamedsTraits
    {
        //3642 is a randomly chosen four digit number to form the first four digis of the trait enum, with the last digit simply counting up with each trait added.
        public static Trait BannedFromCampfire = (Trait)36421;
        public static Trait BannedFromBonelord = (Trait)36422;
        public static Trait BannedSigilTransferVictim = (Trait)36423;
        public static Trait BannedSigilTransferTarget = (Trait)36424;
        public static Trait Obelisk = (Trait)36425;
        public static Trait NoBones = (Trait)36426;
        public static Trait DoubleAllStrikes = (Trait)36427;
        public static Trait InherentRepulsive = (Trait)36428;
    }
}
