using DiskCardGame;
using System;
using System.Collections.Generic;
using System.Text;
using InscryptionAPI;
using InscryptionAPI.Card;
using InscryptionAPI.Pelts;

namespace NevernamedsSigils
{
    /*
    public class Pelts
    {
        private void CustomPelts()
        {
            PeltManager.New(new PeltManager.CustomPeltData()
            {
                AbilityCount = 1,
                CostCallback = () => 0,
                AvailableAtTrader = false,
                CardNameOfPelt = "pelt name",
                MaxChoices = 4,
                PluginGUID = "mod guid",
                GetChoicesCallback = () =>
                {
                    return CardManager.AllCardsCopy.FindAll((a) =>
                        a.temple == CardTemple.Nature &&
                        a.metaCategories.Contains(CardMetaCategory.Rare));
                },
            });
        }
    }*/
}
